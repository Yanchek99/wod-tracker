= simple_form_for @program, wrapper: :input_group do |f|
  = f.error_notification
  .form-inputs.row
    .col-12 = f.input :name
  .form-actions.d-grid.gap-2
    = f.button :submit, class: 'btn btn-success'
    = link_to 'Cancel', @program, class: 'btn btn-secondary'
