
<%= simple_form_for(@workout) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :rounds %>
    <%= f.input :time %>
    <%= f.input :interval %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
